import pandas as pd

class preprocess:

    def basic(data):
        prep_data = data.copy()
        return(prep_data)

    def etc(data):
        prep_data = data.copy()
        return(prep_data)
    
class ml_models:

    def logistic():
        return()
